name: Run Selenium tests

on:
  pull_request:
    types: [ opened, synchronize ]
    branches:
      - main
  workflow_dispatch:

jobs:

  e2e:
    name: e2e-run
    uses: jeeshan12/resusable_workflows/.github/workflows/selenium-docker-grid.yaml@feat-selenium-grid-gha
    with:
      chrome-version: "latest"
      firefox-version: "latest"
